# URL del servidor Django (sin barra final). Copiar este archivo a .env.local.
# En desarrollo con Expo Go usá la IP de tu PC en la red wifi (ipconfig -> "Dirección IPv4"),
# no "localhost": en el celular, localhost es el propio celular.
EXPO_PUBLIC_API_URL=http://192.168.0.10:8000
